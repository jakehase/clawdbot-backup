# 2026-02-02 - OpenClaw Migration & System Cleanup

## OpenClaw Migration Complete ✓
- Migrated from Clawdbot to OpenClaw v2026.2.1
- All configs transferred successfully
- Memory search working with local embeddings (no external API needed)

## Configuration Changes
- **Memory search**: provider=`local`, extraPaths includes `knowledge/`
- **Brave Search**: Enabled via `env.vars.BRAVE_API_KEY` (free tier, 2,000 queries/month)
- **Heartbeat**: Disabled (`heartbeat.target: "none"`)

## System Cleanup
- Killed zombie node processes (browserable remnants)
- Removed browserable package
- ~50 defunct processes remain (need container restart to fully clear, harmless)

## Bird CLI Setup ✓
- Installed at `/usr/local/bin/bird`
- Account: **@LeftThe81262** (TheLeftAngle)
- Auth: `AUTH_TOKEN` + `CT0` stored in `env.vars` config
- Commands: `bird read <url>`, `bird search "query"`, `bird user-tweets @handle`
- Purpose: Read X/Twitter content directly (Brave Search can't index X well)

## Key Discoveries
- `tools.web.search.apiKey` config path wasn't being read; `env.vars.BRAVE_API_KEY` works
- Gemini embedding API returned 404; local embeddings work offline (preferred)
- Bird CLI stores config in `~/.config/bird/` (features.json, query-ids-cache.json)

## Next Steps for Jake
1. Provide X cookies (auth_token, ct0) for Bird CLI authentication
2. Consider container restart to clear zombie processes (optional, cosmetic)
